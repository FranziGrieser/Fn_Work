<section class="container back">
  <!-- <div class="row">
    <div class="col-xs-6 col-sm-6 col-md-6">
      <address><strong>BikeBerlin</strong><br>
        Alexanderplatz 44<br>
        444444 Berlin  <br>
        030 12345 234
      </address>
    </div>
    <div class="col-xs-6 col-sm-6 col-md-6">
      <p>Date: <%= Time.zone.now %></p>
      <p>
        Invoice for:
        <% if signed_in? %>
          <%= current_user.email %>
        <% else %>
          <%= "Please log in for E-Mail confirmation" %>
        <% end %>
    </div>
  </div> -->

  <!-- Pay Button -->
  <div>
    <%= form_with(url: '/payments/create') do |form| %>
        <%= render partial: "shared/stripe_checkout_button" %>
    <% end %>
  </div>
</section>
