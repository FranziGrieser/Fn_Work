  <%= form_with(model: user, local: true) do |form| %>
    <% if user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :first_name %>
      <%= form.text_field(:first_name, size: 30, class: "form-control create_input") %>
    </div>

    <div class="field">
      <%= form.label :last_name %>
      <%= form.text_field(:last_name, size: 30, class: "form-control create_input") %>
    </div>

    <div class="field">
      <%= form.label :email %>
      <%= form.email_field :email, class: "form-control create_input" %><br><br>
    </div>

    <div class="actions">
      <%= form.submit(class: "btn btn-light") %>
    </div>
  <% end %>
